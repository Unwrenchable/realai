/**
 * Coder Agent Template
 * Generates and executes code
 */

const coderAgent = {
  name: "coder",
  model: "realai-1.0",
  tools: ["code-generate", "code-execute", "web-search"],
  memory: null,
};

export default coderAgent;
